export enum RoutePath {
  NotFound = '*',
  Main = '/',
  About = '/about',
  Skills = '/skills',
  Projects = '/projects',
  Contacts = '/contacts',
}

export enum Language {
  En = 'en',
  Ru = 'ru',
}
